<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Theme applied to the Android Window while the process is starting -->
    <style name="LaunchTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started. -->
    <style name="NormalTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
    
    <!-- A clean theme specifically for the SDK's activity -->
    <style name="SDKTheme" parent="Theme.MaterialComponents.Light.NoActionBar" >
        <!-- 1. Opt out of Android 15 edge-to-edge enforcement --><item name="android:windowOptOutEdgeToEdgeEnforcement" tools:targetApi="35">true</item>

        <!-- 2. Set this to false to stop content from going behind -->
        <item name="android:windowTranslucentStatus">false</item>

        <!-- 3. Ensure the window fits system windows -->
        <item name="android:fitsSystemWindows">true</item>

        <item name="android:statusBarColor">@android:color/black</item> <!-- Or your preferred color -->
        <item name="android:windowFullscreen">false</item>
    </style>
</resources>
